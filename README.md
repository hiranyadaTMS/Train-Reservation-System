# Train-Reservation-System
Train Reservation System built with Java and MySQL, utilizing Object-Oriented Programming principles. The project follows the MVC architecture to separate concerns between data, logic, and UI. It also integrates a Singleton-based database connection manager for optimal resource utilization and thread-safe access.
